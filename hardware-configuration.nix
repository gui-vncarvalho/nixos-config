# Este arquivo é específico de cada máquina.
# No ISO, gere com:
#   sudo nixos-generate-config --root /mnt
# e substitua este arquivo pelo gerado antes de rodar nixos-install.

{ ... }:

{
  # Placeholder versionado para o flake sempre enxergar o caminho.
}
